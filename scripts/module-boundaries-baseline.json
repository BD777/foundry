{
  "storeInterfaceMethods": 94,
  "violations": [
    "worker: evidence-agent (issue) loads @anthropic-ai/claude-agent-sdk",
    "worker: evidence-agent (issue) loads @openai/codex-sdk",
    "worker: evidence-agent-sandbox (sandbox) imports execution-storage (candidate-store)",
    "worker: evidence-command-sandbox (sandbox) imports evidence-collectors (issue)",
    "worker: evidence-http-service (issue) spells sandbox-exec",
    "worker: execution-sandbox (sandbox) imports execution-storage (candidate-store)",
    "worker: execution-sandbox (sandbox) imports execution-types (candidate-store)",
    "worker: profiles (harness-profiles) imports session-ambient (session-runtime)"
  ]
}
