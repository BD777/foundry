# Foundry Docker 部署环境变量 —— 复制为 deploy/.env 后按实际修改。
# 浏览器用 Foundry 账号登录：首次启动时服务端日志会打印一次性启动码，
# 打开网页用它创建第一个 Admin 账号（或在容器内运行 /app/foundry-server users create）。

# worker 首次启动用的一次性配对 token：创建 Admin 后在网页 Devices → Add device
# 生成（或 server 容器内运行 /app/foundry-server devices pairing-token --username <名字>）。
# 配对成功后设备凭证保存在 worker 卷里，这个值之后可以留空。
FOUNDRY_PAIRING_TOKEN=

# 浏览器访问地址，必须与你实际打开网址的「协议://主机:端口」完全一致，
# 否则浏览器会被 CORS 拦截。不要混用 IP 和主机名。
FOUNDRY_WEB_ORIGIN=http://<server-host>:31982

# 容器内 worker 调用 Claude 使用的 API Key（claude CLI / SDK 使用）。
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# 宿主机上的工作区根目录（两个容器共同挂载到 /workspace，路径必须一致）。
FOUNDRY_WORKSPACE_DIR=/srv/foundry/workspaces
