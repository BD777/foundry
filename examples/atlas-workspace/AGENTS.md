# Atlas Workspace

This example workspace is used to exercise the Foundry issue loop.

Workers should treat workspace context as authoritative, use installed skills explicitly, and avoid durable worker memory.
